[pytest]
# Scope default collection to the tests/ package. Root-level test_*.py and
# smoke_*.py files are standalone manual scripts and must not be collected
# by a bare `pytest` run (they execute real network/TTS/subprocess work).
testpaths = tests