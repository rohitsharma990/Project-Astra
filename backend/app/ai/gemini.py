from typing import Callable, List, Dict


class GeminiClient:
    def ask(self, user_input: str, history: List[Dict[str, str]]) -> str:
        raise NotImplementedError("Gemini support is not implemented yet.")
